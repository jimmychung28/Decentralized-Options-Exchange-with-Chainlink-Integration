{
  "name": "defi-options-3",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ defi-options-3",
    "create-local": "graph create --node http://localhost:8020/ defi-options-3",
    "remove-local": "graph remove --node http://localhost:8020/ defi-options-3",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 defi-options-3"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.32.0",
    "@graphprotocol/graph-ts": "0.27.0"
  }
}
