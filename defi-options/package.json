{
  "name": "defi-options",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ defi-options",
    "create-local": "graph create --node http://localhost:8020/ defi-options",
    "remove-local": "graph remove --node http://localhost:8020/ defi-options",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 defi-options"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.32.0",
    "@graphprotocol/graph-ts": "0.27.0"
  }
}
